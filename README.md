hello

How the hell does this work:

I take training data which is usually in segments, then i join with --combine

This results in a combined_features.csv and combined_labels.csv

This is then trainined::w
